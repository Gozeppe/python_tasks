# 🧩 Задача: Плейлист и Треки

# Сделай два класса:

# Track

# поля: title, artist, duration_sec (целое число в секундах).

# метод info() — возвращает строку вида:
# "{title} — {artist} ({mm}:{ss})", где минуты/секунды с ведущим нулём (например, 03:07).

# Playlist

# поле: список треков.

# методы:

# add_track(track) — добавляет трек в плейлист.

# find_by_artist(artist) — возвращает список треков этого артиста.

# list_tracks() — печатает все треки (по одному в строке, используя info() трека).

# total_duration() — возвращает общую продолжительность плейлиста в секундах.

# remove_by_title(title) — удаляет первый трек по названию; если не найден — ничего не делает.

# Требования/нюансы

# Плейлист должен хранить треки внутри объекта (через self.tracks, например).

# find_by_artist не печатает, а возвращает список. Печатай снаружи.

# list_tracks() именно печатает строки info() — чтобы проверить формат вывода.

# duration_sec — всегда int. Подумай, где и как это обеспечить.

# В Track.info() следи за форматированием времени (ведущие нули, 60 секунд = 01:00).

# Плейлист можно создавать пустым.